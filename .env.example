# API Configuration
# URL base da API RAG - usado pelo assistente virtual
# API Configuration
# URL da API do assistente
# Em desenvolvimento: o sistema detecta automaticamente URLs https e usa proxy local
# Em produção: usa a URL direta
VITE_API_BASE_URL=https://localhost:8000

# Docker Environment
# URL da API quando rodando em containers Docker
API_BASE_URL=http://host.docker.internal:8000

# Development
NODE_ENV=development
